apiVersion: config.openshift.io/v1
kind: ClusterVersion
metadata:
  name: version
spec:
  upstream: "http://update-service.apps.{{ cluster_name }}.{{ unid }}.{{ base_domain }}/api/upgrades_info/v1/graph"
